<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>
    
    <style name="Theme.Translucent.NoTitleBar.Fullscreen" parent="android:Theme.Translucent.NoTitleBar.Fullscreen">
        <item name="android:background">#3AAA</item>
    </style>
    
    <style name="titleStyle">
        <item name="android:background">@color/title_bg</item>
        <item name="android:textSize">18sp</item>
        <item name="android:typeface">serif</item>
    </style>
    
    <style name="buttonStyle" parent="@android:attr/buttonStyle">
		<item name="android:textColor">@color/button_selector</item>        
    </style>

    <color name="clolor_369">#369</color>
    <color name="clolor_DDD">#DDD</color>
</resources>